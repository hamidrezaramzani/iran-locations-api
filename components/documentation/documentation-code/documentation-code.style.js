import styled from 'styled-components';

export const DocumentationCode = styled.div`
  position: relative;
  .copy-button {
    position: absolute;
    top: 5px;
    right: 5px;
  }

  .copy-button-icon {
    font-size: 20px;
  }
`;
